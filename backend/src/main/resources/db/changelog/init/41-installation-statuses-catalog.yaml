databaseChangeLog:
  - changeSet:
      id: create-installation-statuses-catalog
      author: AALEXEEN
      changes:
        - createTable:
            tableName: installation_statuses_catalog
            columns:
              # ============================================================
              # BaseEntity fields
              # ============================================================
              - column:
                  name: id
                  type: UUID
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: created_at
                  type: TIMESTAMP
                  defaultValueComputed: "CURRENT_TIMESTAMP"
                  constraints:
                    nullable: false
              - column:
                  name: updated_at
                  type: TIMESTAMP
                  defaultValueComputed: "CURRENT_TIMESTAMP"
                  constraints:
                    nullable: false

              # ============================================================
              # NamedEntity fields
              # ============================================================
              - column:
                  name: name
                  type: VARCHAR(128)
                  constraints:
                    nullable: false
              - column:
                  name: description
                  type: TEXT

              # ============================================================
              # BaseCodedEntity fields
              # ============================================================
              - column:
                  name: code
                  type: VARCHAR(64)
                  constraints:
                    nullable: false
              - column:
                  name: display_name
                  type: VARCHAR(128)
                  constraints:
                    nullable: false
              - column:
                  name: is_active
                  type: BOOLEAN
                  defaultValueBoolean: true
                  constraints:
                    nullable: false
              - column:
                  name: sort_order
                  type: INTEGER
                  defaultValueNumeric: 0

              # ============================================================
              # InstallationStatusEntity specific fields
              # ============================================================

              # Status characteristics
              - column:
                  name: is_physically_present
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: is_operational
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: requires_attention
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: is_final_status
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: is_error_status
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false

              # UI representation
              - column:
                  name: color_category
                  type: VARCHAR(16)
                  defaultValue: "secondary"
                  constraints:
                    nullable: true
              - column:
                  name: icon_class
                  type: VARCHAR(64)
                  constraints:
                    nullable: true

              # Status workflow
              - column:
                  name: status_order
                  type: INTEGER
                  defaultValueNumeric: 50
                  constraints:
                    nullable: true
              - column:
                  name: auto_transition_minutes
                  type: INTEGER
                  constraints:
                    nullable: true

              # Business rules
              - column:
                  name: allows_equipment_operation
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: allows_maintenance
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: requires_documentation
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: notifies_stakeholders
                  type: BOOLEAN
                  defaultValueBoolean: false
                  constraints:
                    nullable: false

        # Unique constraint for code
        - addUniqueConstraint:
            tableName: installation_statuses_catalog
            columnNames: code
            constraintName: uk_installation_status_code

        # Indexes
        - createIndex:
            tableName: installation_statuses_catalog
            indexName: idx_installation_status_active_sort
            columns:
              - column:
                  name: is_active
              - column:
                  name: sort_order
                  descending: false

        - createIndex:
            tableName: installation_statuses_catalog
            indexName: idx_installation_status_order
            columns:
              - column:
                  name: status_order

  # Allowed transitions stored by status code (no self-referencing table)
  - changeSet:
      id: create-installation-status-allowed-transitions
      author: AALEXEEN
      changes:
        - createTable:
            tableName: installation_status_allowed_transitions
            columns:
              - column:
                  name: installation_status_id
                  type: UUID
                  constraints:
                    nullable: false
              - column:
                  name: to_status_code
                  type: VARCHAR(64)
                  constraints:
                    nullable: false

        - addForeignKeyConstraint:
            baseTableName: installation_status_allowed_transitions
            baseColumnNames: installation_status_id
            constraintName: fk_installation_status_allowed_transition
            referencedTableName: installation_statuses_catalog
            referencedColumnNames: id
            onDelete: CASCADE

        - addUniqueConstraint:
            tableName: installation_status_allowed_transitions
            columnNames: installation_status_id, to_status_code
            constraintName: uq_installation_status_allowed_transition_pair

        - createIndex:
            tableName: installation_status_allowed_transitions
            indexName: idx_installation_status_allowed_transition
            columns:
              - column:
                  name: installation_status_id
